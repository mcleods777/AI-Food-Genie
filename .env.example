DATABASE_URL="file:./dev.db"

# AI Providers (configure at least Gemini for AI features, app works with mock data otherwise)
# Consolidated Gemini-only architecture: Gemini Flash handles ALL tasks
#   - Item detection/scanning (bounding boxes, speed, cost)
#   - Expiration date OCR (lowest edit distance on OmniDocBench)
#   - Recipe extraction and meal suggestions
# Claude and GPT-4o are optional fallbacks if Gemini is unavailable.

GEMINI_API_KEY="your-gemini-api-key-here"
CLAUDE_API_KEY="your-claude-api-key-here"
OPENAI_API_KEY="your-openai-api-key-here"
